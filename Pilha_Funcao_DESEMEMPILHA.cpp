
                   Desempilhar (Pop)

//implementacao
resp=desempilha(pilha,topo,val)

if(resp==0)
   cout<<"\nAtencao, pilha vazia\n";
else
   cout<<"\nValor removido: "<<val;


//funcao
int desempilha(int[], int &t, int &v)
{
   if(t==-1)
      return 0;
   else
   }
      v=p[t]; //guarda o valor do topo
      t--; //atualiza o topo
      return 1;
   } 
}